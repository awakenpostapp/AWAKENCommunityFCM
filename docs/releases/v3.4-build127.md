# AWAKEN Community FCM v3.4 — build 127

## Giao diện Option 3 · Học viện cộng đồng

- Màu kem, xanh rừng và điểm nhấn đồng; font Nunito Sans, bộ icon Tabler đồng nhất.
- Tổng quan Founder: hình ảnh học viện, yêu cầu cần duyệt, lớp học hôm nay và lối vào Thành viên.
- Điểm danh Coach: tìm học viên, thống kê nhanh, trạng thái gọn, Lưu nháp trên thanh tiêu đề và nút Hoàn tất cố định. Lưu đủ danh sách lớp ngay cả khi đang lọc tìm kiếm.
- Thành tích Trainee: điểm tích lũy cá nhân, biểu trưng ba cột, lịch sử và thông báo đổi quà sắp ra mắt. Giữ nguyên 21 biểu trưng và quy tắc điểm/30 ngày.
- Giữ màn hình khi chuyển tab; bổ sung lối vào Thành viên và Thông báo khi sắp xếp lại thanh điều hướng.
- Sửa lỗi bố trí biểu trưng, khôi phục trang sau khi tải lại thành công và chặn lưu điểm danh khi danh sách chưa tải xong.

## Kiểm tra và cập nhật

- 63 kiểm tra backend và 6 kiểm tra UI đạt; build Release, quét secret và xác minh chữ ký đạt.
- Kiểm tra giao diện/thao tác Android bằng dữ liệu mẫu cách ly: tìm kiếm, lưu nháp/gửi đủ 18 học viên, trạng thái thành tích trống/lỗi, lịch sử và cỡ chữ 130%.
- APK Release cài đặt và khởi động tới màn hình đăng nhập thành công. Chưa kiểm thử mọi nghiệp vụ bằng account production hoặc trên mọi thiết bị thực.
- Giữ version **3.4**, tăng build **127**. Giữ application ID và chứng chỉ ký của build 126; có thể cài cập nhật, không cần gỡ app.
- Không thay đổi hoặc deploy Worker/database production trong bản giao diện này.

Tải file `AWAKENCommunityFCM-v3.4-build127-Release.apk`. File `.sha256` đi kèm dùng kiểm tra tính toàn vẹn.
