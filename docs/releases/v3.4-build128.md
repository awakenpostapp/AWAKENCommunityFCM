# AWAKEN Community FCM v3.4 — build 128

## Cập nhật

- Khi chuyển sang tab khác rồi quay lại, tab luôn trở về trang Home gốc; trang
  con và trạng thái điều hướng tạm thời không còn được giữ lại.
- Bổ sung vị trí Coach **Trợ lý Huấn luyện viên (Assistant Coach)** và
  **Thực tập sinh (Intern)**. Thực tập sinh vẫn có thể được phân công vào lớp
  để theo dõi nhưng không phát sinh lương; quy tắc được áp dụng ở cả UI,
  check-in và các phép tính lương trên Worker.
- Bật xoay màn hình theo cảm biến trên Android (`fullSensor`). iOS giữ hỗ trợ
  xoay ngang trái/phải đã có sẵn.
- Giữ version hiển thị **3.4**, tăng Android build từ 127 lên **128**; giữ
  nguyên application ID và chứng chỉ ký để cài cập nhật trên bản trước.

## Kiểm tra và artifact

- Worker typecheck/build và **65/65** test backend đạt.
- **9/9** UI checks đạt; MAUI Android app và UiHarness build thành công.
- APK Release xác nhận package `com.awaken.communityfootballclubmanager`,
  versionCode `128`, versionName `3.4`, orientation `fullSensor`.
- `apksigner` xác nhận chữ ký v1/v2/v3 hợp lệ; certificate SHA-256 giữ
  nguyên `98cd363b8a135b597393458fbd9e688601793186bcaa1520c446b27bbcf2e19d`.
- Release chỉ phát hành APK và file checksum `.sha256`; không phát hành AAB.
